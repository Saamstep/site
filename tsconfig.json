{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "types": ["styled-jsx"],
    "moduleResolution": "node", // Ensure proper module resolution
    "strict": true, // Optional: Ensures strict type-checking
    "jsx": "react-jsx", // Ensure JSX is set for React 17+ projects
    "noUnusedLocals": false, // Optional: Report errors on unused locals
    "noUnusedParameters": false // Optional: Report errors on unused parameters
  }
}
